import '../styles/ConnectPrompt.css';

export const ConnectPrompt = () => {
    return (
        <div className="connect-prompt">
            <h2>Welcome to SUI Faucet</h2>
            <p>Connect your wallet to claim free SUI tokens</p>
        </div>
    );
};